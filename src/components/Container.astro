---
import Img from "./Img.astro";
import "../styles/style.css";

const { kategori, filter } = Astro.props;
console.log("ALLE PRODUKTER**", kategori);
---

<h1>{filter}</h1>

{kategori.map((prod) => <Img produkt={prod} link={"/underside/" + prod.id} />)}
<article></article>

<style>
  h1 {
    display: none;
  }
</style>
